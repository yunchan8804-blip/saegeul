# 새글 (Saegeul) — Claude Code 진입 지침

이 저장소의 정본 지침은 루트의 `AGENTS.md` 하나다. 이 파일에는 규칙을 쓰지 않는다.

- 세션을 시작하면 **무조건** `AGENTS.md`를 먼저 읽고, 그 안의 "AI 에이전트 오케스트레이션 바이블"과 "새글 저장소 적용 규격"을 절대 규칙으로 따른다.
- Claude Code 전용 실행 규격은 `AGENTS.md` 8.2절에 있다.
- 지침을 고칠 일이 있으면 `AGENTS.md`를 고친다. 이 파일은 포인터로만 유지한다.

@AGENTS.md
