# 한국어 문맥 추천 재개 기록 (2026-09-08)

## 마지막 세션의 확정 상태

- 이전 작업 제목은 `회의 참석 문구 추천 오류 수정`이고 작업 id는 `01a07b80-4b9a-7603-9fb8-61fa0418c876`이다. 마지막 턴은 중단되었다.
- 기준 브랜치는 `YunChan/chanpaca-product-split`이다. 현재 작업 트리의 기존 변경은 보존하며, 이 재개 작업은 커밋·push·배포를 하지 않는다.
- `.artifacts/contextual-quality-e2e/kanana-live-device-run1.log:1-47`은 `KananaLiveContinuationDeviceTest` 1건의 HTTPS 3회와 IME 직접 삽입을 기록한다. 세 입력의 최종 editor text와 typed wire는 확인됐지만, 이는 후보 칩을 누른 결과가 아니다.
- 당시 기본 direct 모드 테스트는 후보 chip UI 클릭과 일반 모델 품질을 범위 밖으로 둔다([KananaLiveContinuationDeviceTest.kt](../app/src/androidTest/java/org/fcitx/fcitx5/android/KananaLiveContinuationDeviceTest.kt)). 따라서 당시 editor 가림으로 화면 가시성 검증을 수용할 수 없고, 칩 미클릭 상태에서 추천 수락·저장·통계 갱신을 완료로 처리하지 않는다. 이후 추가된 click 모드의 결과는 별도 실행 근거가 필요하다.

## 재개 순서와 산출물

1. debug host 상단의 8개 버튼 행이 editor를 가리는 원인은 run3의 실제 HTTPS 1회 뒤 `editor=[27,592][941,2358]`, `visibleWindow=[0,95][968,1313]`에서 확인했다. `systemBars|ime` union padding 수정 후 run4에서 editor와 후보 칩의 동시 가시성을 확인했다. 이는 Android의 [SOFT_INPUT_ADJUST_RESIZE](https://developer.android.com/reference/android/view/WindowManager.LayoutParams#SOFT_INPUT_ADJUST_RESIZE) 및 [software keyboard layout](https://developer.android.com/develop/ui/views/layout/sw-keyboard) 문서와 맞춘 host inset 처리다.
2. live HTTPS 응답을 공개 prefetch cache seam에 전달한 뒤, 실제 accessibility 후보 칩 클릭 3회를 실행했다. 원문 prefix 보존, 공백 경계, 정확히 한 번 삽입은 화면과 editor 값으로 확인했다. suffix와 결합 방식의 계약은 [품질 계획:26-35](korean-contextual-prediction-quality-plan.md#L26)이다.
3. 실제 표시→성공 선택→저장 뒤 앱을 다시 열어 대시보드의 수락률 표시까지는 확인했다. `추천 수락률`은 화면에 실제로 보인 추천을 분모, 성공 선택을 분자로 센다([품질 계획:39-47](korean-contextual-prediction-quality-plan.md#L39)). 이번 수락량 계측은 학습량 카운터 검증이 아니다. 실패·중복 사례를 포함한 전체 경로는 남아 있다.
4. boundary-regression은 처음 잘못된 runner 패키지로 instrumentation 오류가 났으나, 패키지 정정 재시도는 기존 기기 계약 8건 `OK`(118.985초, retry exit 0)로 끝났다. 이는 B23/B26의 좁은 경계 계약 증거이며 전체 편집 품질이나 실제 한글 타이핑 E2E로 확대하지 않는다. 기존 coldstart 2건 실패 기록은 유지한다.

새 실행 산출물은 `.artifacts/contextual-quality-e2e/resume-20260908/`에만 둔다. canonical adb는 `C:/Users/encep/AppData/Local/Android/Sdk/platform-tools/adb.exe`이며, 유선 기기는 Fold6 `SM-F956N` / `R3CX70NE9VH`다. arm64 insets 빌드는 `BUILD SUCCESSFUL`(40초), 종료 코드 0이고 app/test 설치는 각각 성공했다. `emulator-5580`은 종료했고 reverse도 제거했다.

## 현재 검증 증거

- 최종 AI 단위 회귀는 549 tests 중 failures 2, errors 0, skips 7, 종료 코드 1, `BUILD FAILED in 1m 13s`다. 두 실패는 기존 coldstart 항목이며 수정하거나 skip으로 바꾸지 않았다. 최초 권한 오류 로그도 보존했다([verification-summary.md](../.artifacts/contextual-quality-e2e/resume-20260908/verification-summary.md)).
- 권한 상승 companion 실행은 26 tests `OK`, 종료 코드 0이고, continuation surface는 11 tests `OK`, 종료 코드 0이다. 이는 각각 companion 스크립트와 surface 계약의 결과이며 live chip·metrics E2E를 대체하지 않는다([ai-provider-companion-escalated-summary.txt](../.artifacts/contextual-quality-e2e/resume-20260908/ai-provider-companion-escalated-summary.txt), [korean-continuation-surface-summary.txt](../.artifacts/contextual-quality-e2e/resume-20260908/korean-continuation-surface-summary.txt)).
- x86 app·hangul·AndroidTest 빌드는 `BUILD SUCCESSFUL`(31초), 종료 코드 0으로 수용했지만 설치하거나 실행하지 않았다. localhost Kanana run2도 모델 로드와 health 200까지만 확인했고 실제 generation은 0건이며 Ctrl+C로 종료했다. 어느 쪽도 E2E 결과가 아니다.
- 유선 run4는 [kanana-wired-device-run4.log](../.artifacts/contextual-quality-e2e/resume-20260908/kanana-wired-device-run4.log)에서 HTTPS 3회, 실제 accessibility chip 클릭 3회, `OK (1 test)` 29.108초를 기록한다. 클릭 뒤 editor는 `[27,687][941,1295]`, visible window는 `[0,95][968,1313]` 안에 있었고, before-click 3장과 after-click 3장을 직접 확인했다. 최종 memory/disk 계측은 accepted 3, shown 14로 일치한다.
- client 경과 시간은 1,578/634/398ms다. 이는 PC GPU 추론과 USB HTTPS를 포함하며 온디바이스 추론 속도가 아니다. 첫 문장은 `내가 뭘 하려고 했지?`로 삽입됐으나 회의 결과는 두 문장이어서 일반 문장 품질 완료 근거가 아니다. 실제 generation raw는 그대로 쓰고 공개 cache seam만 전달했다. 이 run4 cache seam은 자동 provider prefetch 근거가 아니며, 기본 제품 모델 통합과 일반 품질 완료도 선언하지 않는다. run4 서버는 accepted generation 3건 뒤 정상 종료했고 server exit code는 0이다.
- metrics 표시 변경 빌드는 `BUILD SUCCESSFUL`(19초), 종료 코드 0이고 유선 update-install도 성공했다. 앱 Main 재실행 뒤 「상세 그래프 보기」를 실제로 눌러 [dashboard-after-restart2.xml](../.artifacts/contextual-quality-e2e/resume-20260908/dashboard-after-restart2.xml)에서 수락률 21%와 `표시한 추천 14개 중 3개를 선택했어요. 추천이 표시되거나 선택되면 갱신됩니다.` 문구를 확인했다. 설명 bounds는 `[72,1262][896,1340]`이며, [dashboard-after-restart-cover.png](../.artifacts/contextual-quality-e2e/resume-20260908/dashboard-after-restart-cover.png)에서 비절단 상태와 최근 30일 그래프 7문장·하루 최대 7문장을 확인했다. 이 7문장은 run4 추천의 학습 인과나 원문별 대응 증거가 아니다. `dashboard-after-restart.png`는 multiple display 경고가 있는 실패 캡처이고 display0은 비활성 내부 화면이므로 증거에서 제외한다.
- 초기 `AutomaticContinuationDeviceTest` 실행은 14.673초에 실패 1건이었다. `✨` 단어 행을 문장 응답 완료로 오인해 snapshot을 조기 검증한 테스트 결함이며, 실패 캡처에는 두 행이 보이기도 했으므로 이를 제품의 문장 0개나 positive 자동 prefetch 품질 판정으로 쓰지 않는다. positive 테스트는 cache 주입 없이 실제 cache의 `CONTINUATION`과 같은 텍스트의 clickable IME 노드를 함께 기다리도록 고쳤다.
- 실제 마지막 자동 dispatch는 `03:51:27.444`에서 시작해 `03:51:27.450`에 `AiReauthenticationRequiredException`으로 끝났다. 이 시점에 root는 `oauth-session.bin`이 없음을 직접 확인했으며 내용은 읽지 않았다. `04:00` 재확인 때는 파일이 2,064 bytes로 생성돼 있었다. 이는 사용자 로그인 진행을 반영한 것일 수 있으나, 파일 존재만으로 API 유효성을 단정하지 않는다. profile의 모델·이름 변경에도 session을 지우던 코드 결함은 수정됐지만, 이번 session 유실 원인을 그 결함으로 단정하지 않는다. missing OAuth session의 실제 오류 안내 negative 기기 테스트는 추가됐으나 실행하지 않으며, 다음 기기 검증은 positive 자동 prefetch를 선택한다.
- [enrichment-async-auth-build.log](../.artifacts/contextual-quality-e2e/resume-20260908/enrichment-async-auth-build.log)는 통합 빌드 `BUILD SUCCESSFUL` 51초, exit 0을 기록한다. OAuth refresh 12·identity 4·prefetch 22·status 5·enricher 11·graph store 19의 6 XML, 총 73 tests는 failures 0·errors 0·skips 0이다. app/test 유선 update-install도 각각 성공했다.
- [automatic-continuation-auth-restored.log](../.artifacts/contextual-quality-e2e/resume-20260908/automatic-continuation-auth-restored.log)는 `AutomaticContinuationDeviceTest` 1건 `OK`, 56.203초, exit 0이다. cache missing poll 166회로 약 41.5초를 기다린 뒤 `sentenceCandidateCount=1`, source `llm_cached`를 기록했고, [automatic-continuation-65246459.png](../.artifacts/contextual-quality-e2e/resume-20260908/automatic-continuation-65246459.png)에서 합성 `내일 판교에서 ` 뒤 `만나서 얘기해요.` 문장 후보가 보이는 것을 root가 확인했다. [automatic-prefetch-timing-safe.txt](../.artifacts/contextual-quality-e2e/resume-20260908/automatic-prefetch-timing-safe.txt)는 원문 응답·토큰 없이 SaegeulAI timestamp만 추출해 `04:26:15.602` dispatch→`04:26:56.266` result, 40,664ms를 기록한다. 약 41.5초 대기와 맞지만 CLI 내부 원인은 확인되지 않았다. 검증 APK SHA-256은 `5DBFCB57F2B67417D3E0253FAD9137060C86C1A688260E6BA3EC696C653E9623`이다. 이는 일반 provider의 자동 cache→UI 표시 경로 1건만 입증한다. 실제 선택, 다른 앱, 일반 문장 품질, provider 대기 시간 개선은 검증하지 않았다.
- [dashboard-responsiveness-device.log](../.artifacts/contextual-quality-e2e/resume-20260908/dashboard-responsiveness-device.log)는 1건 `OK`, 22.348초, exit 0이다. startup 13,291ms/max main queue 259ms와 analysis 7,309ms/max main queue 7ms(72 samples)를 기록했고 busy 표시 및 Sync/Clear 비활성화와 completion을 통과했다. [typing-dna-dashboard-startup-65310001-1.png](../.artifacts/contextual-quality-e2e/resume-20260908/typing-dna-dashboard-startup-65310001-1.png), [typing-dna-dashboard-analysis-65317464-2.png](../.artifacts/contextual-quality-e2e/resume-20260908/typing-dna-dashboard-analysis-65317464-2.png)에서 04:01의 LLM 완료·10 nodes·9 relations·3 topics 및 분석 문장 112→113 표시를 확인했다. 이 기기 테스트는 새 LLM 완료까지 기다린 증거가 아니다. IO 비동기 전환은 검증했으며, 전체 로딩 13초와 provider 대기의 시간 단축은 남아 있다.
- 마지막 실제 금고 강화는 사용자가 실패를 보고했고, 구형 기기 저장 상태에서 `04:36:36` 시작→`04:37:25` 실패(49.7초), `FAILED`만 기록됐다. 구형 상태에는 세부 실패 이유가 없어 이 실행의 원인은 미확정이다. [graph-enrichment-completion-run2.log](../.artifacts/contextual-quality-e2e/resume-20260908/graph-enrichment-completion-run2.log)는 1 test, 72.786초, failure 1을 기록하며, 이전 성공 메타인 10 nodes·9 edges·3 topics와 새 실행의 started/finished/lastApplied를 분리해 출력한다. 마지막 성공 시각과 이 graph 메타는 보존하지만, 이번 실행의 LLM 강화 성공으로 바꾸지 않는다.
- 강화 상태의 failure enum·원인 UI·graph 3-array 검증·terminal 기기 검증을 추가했다. 최초 빌드는 권한 및 AAPT apostrophe 오류로 실패한 기록을 보존했고, [enrichment-failure-detail-build-run2.log](../.artifacts/contextual-quality-e2e/resume-20260908/enrichment-failure-detail-build-run2.log)는 정정 뒤 `BUILD SUCCESSFUL` 30초, exit 0이다. 4 XML의 42 tests( runner 3·status 7·enricher 13·store 19)는 failures 0·errors 0·skips 0이며 app/test 설치도 성공했다. `graph-enrichment-completion-run1`은 `am` 인자 오류로 실행되지 않았고, run2가 실제 terminal 실행이다.
- 이후 새 failure enum으로 재현한 강화는 `04:56:08` 시작→`04:56:59.827`에 `FAILED/PROVIDER_ERROR`가 됐다. root가 허용 목록 SaegeulAI logcat 메타만 읽은 [graph-enrichment-failure-http502.txt](../.artifacts/contextual-quality-e2e/resume-20260908/graph-enrichment-failure-http502.txt)는 HTTP 502와 51,787ms를 기록한다. health는 ok이고 public manifest의 `fast=agy` 매핑만 확인했다. 설치된 script의 graph wrapper가 남아 있어 구버전 원인으로 단정하지 않는다. 응답 본문·자격증명·개인 입력은 수집하지 않았다.
- `probe-installed-graph.py`의 합성 20문장 AGY 1회 진단은 처음 자동 승인 심사에서 내부 graph 지침의 외부 전송 위험으로 거절됐다. 이후 사용자의 명시 승인을 받아 root가 require_escalated로 한 번 실행했다. [graph-probe-approved-run1.json](../.artifacts/contextual-quality-e2e/resume-20260908/graph-probe-approved-run1.json)은 결과를 기록했고, 프로세스 exit 0은 [graph-probe-approved-run1.exit](../.artifacts/contextual-quality-e2e/resume-20260908/graph-probe-approved-run1.exit)에 보존했다. 두 번째 합성 probe 외부 호출은 하지 않는다.
- 승인된 1회 호출은 AGY `gemini-3.8-flash-high`, 54,338ms, `status=completed`, raw length 899를 기록했다. raw JSON parse는 false였지만 CLI→normalizer 경로는 수락됐고 HTTP 502는 재현되지 않았다. 최종 normalized graph를 관측하지 않았으므로 그래프 성공·기기 강화 성공·기존 502 원인·모델 거부 여부는 확정하지 않는다. JSON 메타만 보존하며 원문은 저장하지 않는다. `graph_response_contract`의 normalized shape 검증과 오프라인 1 test(5 table cases)는 0.004초 `OK`, `AST_PARSE_OK`로 완료했다. 이 후속 개선 검사는 이번 외부 실행 결과를 소급 입증하지 않는다.
- 사용자가 세 CLI의 자동 승인 실행을 명시해 repository와 설치본 companion 옵션을 갱신했다. Codex는 `codex exec --yolo`, Claude와 AGY는 `--dangerously-skip-permissions`를 사용하며, 기존 read-only sandbox·`approval_policy=never` 및 Claude safe-mode·tools disabled·`dontAsk` 조합은 제거한다. 로컬 help는 세 옵션 지원을 확인했고 `codex exec --yolo --help`는 exit 0이다. root가 직접 읽은 [companion-permissions-tests-retry.log](../.artifacts/contextual-quality-e2e/resume-20260908/companion-permissions-tests-retry.log)는 mock 28 tests `OK`, 0.064초, exit 0이다.
- root는 설치본을 백업한 뒤 candidate SHA-256 `30C35ED37614568A79F873739620A701954C3B505E1ED5792D107F6AC567F7D6`를 적용하고 재시작을 완료했다(원본 `40FAC3…`). 확인된 실행은 tray PID 11916, wrapper PID 42744, Python PID 31300이며 localhost health는 `ok`/`computer-cli`다. OAuth 파일 삭제·초기화·내용 열람, token 열람, 주소 또는 model 변경은 하지 않았다. 재시작 중 OAuth metadata 길이는 1,054→990으로 변했고 기존 startup expired-grant prune 경로가 있으나, 정확한 변경 내용과 실제 OAuth refresh 성공은 미검증이다. 실제 강화 HTTP 502 해결도 미검증이다. AGY graph probe의 외부 전송 승인은 이 1회 실행으로 소비됐고, 추가 합성 probe 외부 호출에는 새 명시 승인이 필요하다.
- 새 앱 진입·페이지·IME freeze 조사는 이전 Fold6이 아닌 유선 A35 `SM-A356N` / `RFCX60GBL3D`에서 진행한다. 이전 앱 last update는 09-07 22:30이다. 실제 Main 병목은 홈 카드 bind의 force reload와 암호 통계 Main 처리, Privacy summary와 sync의 Main 처리, 4 store save가 같은 object monitor에서 암호화하는 경합, 자동 graph profile load의 Main 처리로 확인됐다. root가 검토·수용한 수정과 경합 재작업을 반영했으나, 이 사실만으로 20초 진입 문제의 해결을 선언하지 않는다.
- [startup-freeze-build-run1.log](../.artifacts/contextual-quality-e2e/resume-20260908/startup-freeze-build-run1.log)는 `BUILD SUCCESSFUL` 1분 49초, exit 0이며 54 tests(4·15·14·8·7·6)는 failures 0·errors 0·skips 0이다. root는 app과 test APK를 `install -r`로 설치해 기존 기기 데이터를 유지했다. `am start` WARM 측정은 전 990/1,001ms, 후 912/918ms이지만 Activity 시작 측정일 뿐 20초 문제 재현 또는 개선 배수의 근거가 아니다.
- [startup-freeze-device-run1.log](../.artifacts/contextual-quality-e2e/resume-20260908/startup-freeze-device-run1.log)는 A35의 2 tests 중 1 failure, 47.247초를 기록했다. Main 첫 UI 조건은 45초 timeout(`totalElapsedMs=46,621`, main queue max 1,056ms, 467 samples)으로 끝나 앱 freeze 확정 근거가 아니다. root는 [startup-freeze-home-current.png](../.artifacts/contextual-quality-e2e/resume-20260908/startup-freeze-home-current.png)에서 홈 카드·numeric·상세 진입이 정상 렌더된 것을 직접 확인했다. run1 실패는 Preference 1.2.1 `onBind`가 root ID를 `mViewId`로 덮는 test selector 결함으로 javap과 live UI child ID로 확인됐다.
- test selector를 `MaterialCard` ancestor로 고쳤고 threshold는 바꾸지 않았으며, app source의 추가 수정은 없다. [startup-freeze-test-build-run2.log](../.artifacts/contextual-quality-e2e/resume-20260908/startup-freeze-test-build-run2.log)는 test APK `BUILD SUCCESSFUL` 19초이고 root의 `install -r -t`도 성공했다. [startup-freeze-device-run2.log](../.artifacts/contextual-quality-e2e/resume-20260908/startup-freeze-device-run2.log)는 `MainActivityResponsivenessDeviceTest` 1 test `OK`, 3.702초를 기록했다: home first UI 1,566ms·data ready 1,567ms(main max 1,092ms, 16 samples), dashboard first UI 774ms(main max 645ms, 8 samples), back 640ms(main max 420ms, 3 samples), total staged 2,995ms다.
- 현재 완료 범위는 코드 병목 수정·설치·이 UI 경로의 범위 검증이다. 20초 간헐 재현은 아직 없고 최대 Main 1,092ms의 trace와 재현 경로 기록이 남아 있다. 모든 앱의 IME freeze가 완전히 해결됐다고 선언하지 않는다.
- 앞선 run1의 `TypingDnaStagingLatencyDeviceTest`는 `PASS`다. live+reload 10건의 raw duration은 `[24,12,9,9,9,8,9,9,9,7]ms`, total 105ms, max 24ms이며 staging 파일 지속도 확인했다. staging의 즉시 write 내구성 계약은 바꾸지 않았다. 개인 금고 데이터의 외부 전송은 없으며, 합성 staging은 기기 cache 임시 파일만 사용한다. staging run2는 재실행하지 않았다.
- read-path 리팩터링 P1은 8 store의 migrate+read를 단일 read와 조건부 migration으로 정리했고 vault 47 tests는 failures 0·skips 0이다. P2는 canonical path generation, source/length/mtime version, repository stats-summary cache, UI force reload 제거를 적용했고 관련 109 tests는 failures 0·skips 0이다. 전체 unit 실행은 1,070 tests 중 기존 문맥 추천 2건(판교 47행·회의 69행), skips 7로 exit 1이었고 `--continue` 뒤 app/test APK는 생성됐다.
- 최종 app SHA-256은 `C5D2F69C7FA47C61256D9D5810AF1D730917A975430138F23C9C1C89AC62E11D`다. 최종 build는 `BUILD SUCCESSFUL` 12초, exit 0이고 root가 A35 `RFCX60GBL3D`에 app/test APK를 설치해 `Success`를 확인했다. [final-read-cost.log](../.artifacts/performance-read-refactor-20260908/final-read-cost.log)의 동일 데이터 지문 baseline은 `5b91d8d9bfdec607bdc4ab695187ae4cb8cfd486bcc105bd9269ff1467cc4290`이며, reader pair 측정은 before first 349ms·repeat 6건 median 26ms에서 final first 150ms·repeat `[18,15,13,13,14,14]ms` median 14ms가 됐다. home warm 0ms는 1ms 미만 floor 표기다. 이는 앱 전체 20초가 아니라 reader pair 비교이며, before cold `am start` 2,025/2,022/1,925ms→중간 after 1,912/1,937/1,869ms는 내부 data ready가 아니고 최종 UI 축소 전 APK 수치다.
- [final-card-geometry.log](../.artifacts/performance-read-refactor-20260908/final-card-geometry.log)는 geometry 1 test `OK`: width 945px, density 2.625, chart `GONE` 0px, card 493px(약 188dp)다. root는 [final-loading-card.png](../.artifacts/performance-read-refactor-20260908/final-loading-card.png)를 직접 확인했다. intermediate geometry test의 Theme 누락 실패는 실제 Material theme로 정정했고, intermediate Main run2 timeout은 CellBroadcast 실종 경보 foreground 가림을 root가 PNG와 focus로 확인해 popup만 확인·닫았으며 알림 설정은 유지했다.
- [final-main-selected-ime.log](../.artifacts/performance-read-refactor-20260908/final-main-selected-ime.log)는 Saegeul IME가 선택된 A35에서 `MainActivityResponsivenessDeviceTest` 1 test `OK`, 4.226초를 기록한다: home first UI 1,645ms·data ready 1,646ms(main max 1,388ms, 17 samples), dashboard first UI 906ms(main max 851ms, 9 samples), back 985ms(main max 586ms, 10 samples), total 3,542ms다. 이전 Main 실패의 추가 원인은 default IME가 HoneyBoard여서 `ACTION_MAIN`이 SetupActivity의 `shouldShowUp`→`SetupPage.Select` 미완료 경로로 가며 홈을 가린 상태였다. root가 adb IME 선택을 Saegeul로 복구하고 Settings에서 확인한 뒤 코드·threshold 수정 없이 통과했다. cold force-stop 뒤에는 default IME 사전조건을 다시 확인해야 한다.
- 현재 완료 범위는 P1/P2와 P3의 선택된 경로 코드·unit·A35 설치·reader/geometry/Main 테스트다. Fold6 `z-fold6` (`100.109.125.97`)에는 root가 13:19:45에 최종 SHA APK `saegeul-debug-read-cache-card-fix-20260908.apk`를 Taildrop 전송해 sent exit 0을 확인했지만, Fold6 설치·런타임 검증은 하지 않았다. 이 리팩터링의 현재 기기 검증은 A35이며, 문서의 Fold6 기재는 이전 실행의 역사 증거다.
- 20초 간헐 진입은 재현되지 않았고 최대 Main 1,388ms의 trace와 재현 경로 기록이 남아 있다. 모든 앱의 IME freeze가 완전히 해결됐다고 선언하지 않는다. 실제 LLM graph 성공, 41초 provider 대기와 B19 등 기존 gate도 남아 있다.

## 백로그별 미완료 범위

- **진행 중 핵심 작업**: 금고 LLM 강화 상태와 일반 채팅·끝 공백 문맥의 문장 후보 0개 문제를 계속 진단한다. UI 상태·store·save 예외 전파와 키보드 provider/OAuth 안내의 통합 빌드·유선 설치는 통과했고, restored session의 positive `AutomaticContinuationDeviceTest`는 일반 provider 자동 cache→UI 표시 1건을 확인했다. 그러나 실제 선택, 다른 앱, 일반 품질, provider·전체 로딩 성능은 미완료이며 missing OAuth session negative 기기 테스트는 session 복구로 실행하지 않았다.
- **B19**: 잘못된 input-continuation 조립 제거는 코드 gate일 뿐이다. 빈 금고·미학습 문맥의 top-k 자연스러움, 실제 표시·선택 E2E가 남아 있다([백로그:81](ai-personalization-backlog.md#L81)). 현재 coldstart 2건 실패는 해소로 바꾸지 않는다.
- **B20**: run4는 표시·선택·저장 계측을 보여 주며 accepted 3·shown 14가 source instance와 disk에서 일치한다. metrics 빌드·유선 update-install 뒤 앱을 다시 열고 대시보드까지 이동해 수락률 21%, 분모·분자 설명 `표시한 추천 14개 중 3개를 선택했어요. 추천이 표시되거나 선택되면 갱신됩니다.`와 그래프 표시를 확인했다. 7문장 그래프는 추천 학습 인과·원문별 대응 증거가 아니다. 실패·중복·재시작 전체 경로는 남아 있다([백로그:82](ai-personalization-backlog.md#L82)).
- **B21**: 작은 화면·큰 글꼴에서의 비절단과 실제 레벨·칭호·진행 근거의 일치가 남아 있다([백로그:83](ai-personalization-backlog.md#L83)).
- **B22**: 어미 추출·저장의 제한된 기기 근거와 별개로, 실제 입력에서 어미 후보·문장 수치·추천 결과를 함께 보이는 경로가 남아 있다([백로그:84](ai-personalization-backlog.md#L84)).
- **B23**: 정정 재시도 boundary 기기 계약에는 외부 커서 이동과 physical delete의 pending 경계 2건이 포함돼 통과했다. 일반 편집 품질·실제 한글 타이핑 E2E와 전체 완료 판단은 보류한다([백로그:85](ai-personalization-backlog.md#L85)).
- **B24**: LLM이 관측 문구·bigram을 대체하지 않는 구현·검증이 진행 중이다. Qwen 및 Polyglot 문장 생성 실험은 제품 도입 반려이며 다음 어절 검증과 구분한다([백로그:86](ai-personalization-backlog.md#L86)).
- **B25**: exact cache 키 단위검증과 설정 readiness만으로 실제 prefetch 성공을 판단하지 않는다. run4는 실제 HTTPS 응답을 공개 cache에 전달한 UI 경로만 검증했다. 별도 restored-session 실행은 일반 provider 자동 cache→UI 표시 1건을 확인했지만, 실제 선택·다른 앱·속도·일반 품질은 미검증으로 남긴다([백로그:87](ai-personalization-backlog.md#L87)).
- **B26**: 정정 재시도 boundary 기기 계약에는 exact/stale/reject 및 attach 경계 6건이 포함돼 통과했다. scope와 `CONTINUATION_ATTACH`의 문맥 적합성·실제 한글 타이핑 E2E·일반 품질 통과는 남아 있다([백로그:88](ai-personalization-backlog.md#L88)).

## 제한 실험과 제품 판단의 경계

2026-09-08 15:31 추가팩 후속: [문장팩 설계·검증 기록](sentence-packs-2026-09-08.md)의 추천 개선 수정본을 A35 유선 설치·기기6건 검증 후 폴드6로 전송했다. 기본216/선택5261은 개인금고와 별도이며 사용자 동의 다운로드 관리다. 공백 뒤 마지막 완성어절의 제한적 조회, 기본/선택팩 매치 병합, badge 표시 및 같은 값의 후보 rebind 클릭 오류를 수정했다. 단위32건 및 실제 오프라인 IME 표시·클릭·한 번 삽입2건 포함 기기6건 통과. 최신 `saegeul-debug-sentence-coverage-fix-20260908.apk` SHA-256 `62272BB589856F652C84B9E3AF0BA60682DDE2587C14A80B35D99693F9B2A6A4`. 폴드6 실행, 일반 문맥 품질, provider 강화 성공·지연은 여전히 미완료다. `안녕하세요`/`지금 뭐해` 등 빈 후보를 보완할 실제 앱·입력 예시가 필요하며 기존 작업을 완료 처리하지 않는다.

Kanana, Qwen, Kiwi, Polyglot 실험은 후보 생성·표면화·품질을 비교하기 위한 제한 실험이다. 저장된 Kanana 출력 재생도 HTTP 파싱과 typed 삽입 문자열의 계약만 보며, live 호출·후보 클릭·실제 IME·성능은 별도 근거가 필요하다([품질 계획:26-30](korean-contextual-prediction-quality-plan.md#L26)). restored-session 자동 prefetch는 일반 provider의 cache→UI 표시 1건이며, 제품 모델 도입, 일반 coldstart 품질 통과, 자동 prefetch 전체 E2E를 선언하지 않는다.

다음 순서는 자동 provider prefetch의 약 41.5초 대기와 문장 0개 진단, 금고 LLM 강화 HTTP 502의 로컬 read-only 원인 분석, 새 명시 승인 뒤의 추가 외부 LLM 재실행, 금고 강화의 새 terminal 성공 확인, B19 coldstart 일반 엔진 제품 통합·품질 평가, B20 실패·중복·재시작 전체 경로, B22 실제 수집→추천이다. coldstart 모델의 일반 품질 완료와 실제 최종 강화 성공은 아직 선언하지 않는다.
